# Budget project — Claude instructies

## Migraties

Maak altijd een **nieuwe migratie** aan voor schemawijzigingen. Gebruik **nooit** `migrate:fresh` of `migrate:reset` — dit vernietigt alle data in de database.
